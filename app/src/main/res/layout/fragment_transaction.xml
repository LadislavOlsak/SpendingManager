<LinearLayout
     xmlns:android="http://schemas.android.com/apk/res/android"
     xmlns:app="http://schemas.android.com/apk/res-auto"
     xmlns:tools="http://schemas.android.com/tools"
     android:id="@+id/constraintLayout"
     android:layout_width="match_parent"
     android:layout_height="match_parent"
     tools:context=".transaction.TransactionActivity$PlaceholderFragment"
     android:orientation="vertical">

    <Button
        android:id="@+id/transaction_add_new_field_btn"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        style="@style/Widget.AppCompat.Button.Borderless.Colored"
        android:text="@string/add_new_field"/>
    <View
        android:id="@+id/divider2"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginBottom="5dp"
        android:background="@color/colorAccent" />

    <ListView
        android:id="@+id/transaction_items_lv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginStart="5dp"
        android:layout_marginEnd="5dp"
        android:divider="@null"
        android:dividerHeight="5dp"/>

    <!--
    add to last elemnet in case toolbar is displayed
        android:layout_marginBottom="?attr/actionBarSize"
        -->

</LinearLayout>